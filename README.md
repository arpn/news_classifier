# Training a sentiment classifier head on top of a pre-trained transformer model

Testing sentiment classification with pre-trained models.
Dataset: ag_news
Model: bert-base-uncased + classification head
